# Amorphous Silicon Structure Configuration
# ==========================================

root_dir: ${oc.env:PROJECT_ROOT,./}/data

# Initialization method: from_cif, random_icp, pymatgen
init: from_cif

# CIF file path
cif_path: ${structure.root_dir}/crystal-structures/c-Si.cif

# Target density for scaling (g/cm³)
target_density: 2.20

# Structure properties
species: [Si]
stoichiometry: [1]

# Random initialization settings (if init=random_icp)
box_length: 25.0

__pycache__/
*.pyc
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
.venv/
venv/
ENV/
env/
.env
.virtualenvs/

# Poetry - DON'T ignore poetry.lock!
# poetry.lock  <- Do NOT add this line

# IDEs
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Project specific
*.egg-info/
dist/
build/
*.egg-info/
.DS_Store
scripts/outputs
scripts/wandb

# Duplicate data subtree (canonical copy is data/)
data/data/

# Local-only files (not for the repo)
CLAUDE.md
Localsymmetrypredictors/

# Local analysis virtualenv (1.3 GB, recreate with `uv venv .venv-analysis`)
.venv-analysis/

# Run artifacts -- regenerate by rerunning; never commit.
# outputs/* rather than outputs/: git will not re-include a file whose parent
# directory is excluded, so the README exception below needs the directory
# itself to stay walkable.
wandb/
outputs/*
!outputs/retired/
outputs/retired/*
!outputs/retired/README.md
